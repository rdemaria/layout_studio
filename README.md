# Layout Studio

Layout Studio models three-dimensional beam-line layouts from reference curves,
reusable component types, and frame-aligned object instances.

The initial Python implementation is in [`python_api/`](python_api/). It
includes the editable model, analytic geometry resolver, scoped Matplotlib and
VTK viewers, examples, design documents, and conformance tests.
